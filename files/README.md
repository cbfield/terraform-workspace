# Files

Files used by the module belong here. They can be accessed from the Terraform configurations at the root of the repository like so:

```hcl
file("${path.module}/templates/my-template.json")
```
